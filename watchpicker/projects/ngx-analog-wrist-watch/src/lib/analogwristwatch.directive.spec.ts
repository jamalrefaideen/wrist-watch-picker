import { AnalogwristwatchDirective } from './analogwristwatch.directive';

describe('AnalogwristwatchDirective', () => {
  it('should create an instance', () => {
    const directive = new AnalogwristwatchDirective();
    expect(directive).toBeTruthy();
  });
});
